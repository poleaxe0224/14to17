{
  "name": "14to17",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/generate-freshness-badge.mjs && vite build && node scripts/generate-share-pages.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "refresh-data": "node scripts/fetch-bls-wages.mjs && node scripts/fetch-cps-earnings.mjs && node scripts/fetch-scorecard-tuition.mjs --force && node scripts/fetch-ipeds-data.mjs && node scripts/fetch-onet-data.mjs"
  },
  "dependencies": {
    "@picocss/pico": "^2.1.1"
  },
  "devDependencies": {
    "jsdom": "^29.0.2",
    "vite": "^6.3.0",
    "vitest": "^3.1.1"
  }
}
